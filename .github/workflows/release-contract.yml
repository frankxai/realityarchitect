name: Release contract

on:
  pull_request:
    paths:
      - ".github/release.yml"
      - ".github/scripts/release-*"
      - ".github/scripts/validate-release-ledger.mjs"
      - ".github/workflows/draft-release.yml"
      - ".github/workflows/release-contract.yml"
      - "CHANGELOG.md"
      - "RELEASING.md"
      - "docs/releases/**"
      - "docs/RELEASE-FOUNDATION-*.md"
  push:
    branches:
      - main
    paths:
      - ".github/release.yml"
      - ".github/scripts/release-*"
      - ".github/scripts/validate-release-ledger.mjs"
      - ".github/workflows/draft-release.yml"
      - ".github/workflows/release-contract.yml"
      - "CHANGELOG.md"
      - "RELEASING.md"
      - "docs/releases/**"
      - "docs/RELEASE-FOUNDATION-*.md"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: realityarchitect-release-contract-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Reality Architect release ledger
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Check out full history
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.0
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"

      - name: Validate ledger
        run: node .github/scripts/validate-release-ledger.mjs

      - name: Test release contract
        run: node --test .github/scripts/release-ledger.test.mjs
